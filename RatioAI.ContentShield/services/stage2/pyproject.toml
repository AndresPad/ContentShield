[project]
name = "contentshield-stage2"
version = "0.1.0"
description = "ContentShield Stage-2 vLLM detector service"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stage2"]
